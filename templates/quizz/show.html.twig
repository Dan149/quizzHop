{% extends 'base.html.twig' %}

{% block title %}Quizz{% endblock %}

{% block body %}
    <h1>Quizz</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ quizz.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ quizz.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ quizz.description }}</td>
            </tr>
            <tr>
                <th>Players</th>
                <td>{{ quizz.players }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('quizz_index') }}">back to list</a>

    <a href="{{ path('quizz_edit', {'slug': quizz.slug}) }}">edit</a>

    {{ include('quizz/_delete_form.html.twig') }}
{% endblock %}
